- git config --global user.name "Maya"

-git log : shows you the git lof /commit history
	- hit q to quit if it's too long
	- git log --oneline: gives short log version

- 'git diff' :  
 

- git checkout <version> from git log - head deattach
- git checkout master : to return head to main (master) branch
